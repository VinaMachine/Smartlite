# Gateway Schema Snapshots

Thư mục này giữ các schema đã được gateway "freeze" để phục vụ backward compatibility. Khi có phiên bản mới trong `/schemas`, hãy copy vào đây, thêm migration trong `../transformers` rồi cập nhật `config.yaml`.
